-- Instagram handle per partner -- dipakai di layar detail partner buat
-- tombol "Copy IG" (di samping tombol WA yang pakai kolom `phone` yang
-- udah ada).
alter table partners add column instagram text;
